# cyberark-ansible-create-unix-user
Create a unix user and onboard into cyberark

## Usage
Modify the hosts file to contain the hostname associated with the target *nix server.
```bash
# now run the playbook
$ ./run_playbook.sh
```
